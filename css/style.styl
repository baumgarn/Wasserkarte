@import "../node_modules/ol/ol.css";

@import url('https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,100..900;1,100..900&family=Work+Sans:ital,wght@0,100..900;1,100..900&display=swap');

@import url('https://fonts.googleapis.com/css2?family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap');


wasserblau = #0082ff
wasserblau = #1b6bb8
wasserblau_light = #cbf3f0
wasserblau_light = #1b6bb848
markerblau = #72bbff
blaugraph = #8ccce3
gruen = #008C46
gruentrans = #008C4666
gruen_high = #008C46
gruen_hi = #afd2ae
uibggrau = #00000020
graublau = #cce7eb

logogruen = #008c46
uigruen = #27975f
uigruen = #579f7b
uigruen = #c4e4d4

//
uibggrau = #ccc
uicardgrau = #eee



:root
	--borderstyle 1px solid #ccc
	--chartborderstyle 1px solid #00000015
	--chartborderradius 4px
	--sidebartextmargin 1px
	--borderstyle none
	--dropshadowfilter drop-shadow(0 2px 3px #00000022)
	--activecolor #b9d0eb
	--activecolordarker #899cb399
	--activecolorgrey #5275a055
	--activecolorgreydarker #5275a088
	--activecolorgreymid #5275a033
	--activecolorgreybrighter #5275a022
	--activecolorgreybrightest #5275a011
	--menusectionheadercolor #4c88ccff
	--menusectionheadercolor rgb(68, 102, 188)
	--menusectionheadercolorlight rgba(68, 102, 188, 1)
	--menusectionheadercolorlighter rgba(68, 102, 188, 0.4)
	--menusectionheadercolorlightest rgba(68, 102, 188, 0.4)
	--warningred  #ea4545ee
	--windowbg #efefef
	--uibrighter #f7f7f7
	--menuactivebg #eaeaea88
	--menuinactivebg #eaeaea
	--menugrau #eee
	--menubariconsize 42px
	--separatorline 2px solid #00000011
	--thinline 1px solid #00000022
	--logogruen #008c46
	--logorot #f15f5c
	--infobg #33333388
	--infobg #22222288
	--timelineheight unquote("min( max( 4vh, 42px), 64px )")
	--timelineheightmobile unquote("max( 4vh, 64px)")
	--timelinedatebg #44444499
	--menuheaderline #00000022

body.is-mobile
	--timelineheight var(--timelineheightmobile)

*
	box-sizing border-box
	scrollbar-width: thin


html, body
	margin: 0;
	height: 100%;
	font-family: "Noto Sans", sans-serif;
	font-family: "Ubuntu", sans-serif;
	font-weight: 500;
	font-weight: 400;
	font-size: 11pt;
	line-height 1.3
	overscroll-behavior: none;
	color: #000000

a
	color: #000000ee

h1, h2, h3, h4, h5
	font-family: "Ubuntu", sans-serif;

.app-container
	position absolute
	right 0
	top 0
	bottom 0

#ui
#app
	visibility hidden

body.loaded
	#ui
	#app
		visibility visible

/////
#map
	position: absolute;
	top: 0;
	bottom: 0;
	left: 0;
	right: 0;

.ol-zoom
	right: 12px !important
	top: 24px !important
	left: unset !important;
	display none
	> *
		width 24px
		height 24px

.sidebaropen .ol-zoom
	right 612px !important

.ol-attribution
	font-family sans-serif
	opacity .8
	z-index 102
	top unset !important
	transform translate(100%, 0) rotate(-90deg)
	transform-origin 0 100%
	border-top-right-radius 6px !important
	border-top-left-radius 0 !important
	ul li
		font-size 7.5pt
		color #333
		a
			color #333
			&:hover
				text-decoration underline

.minimap .ol-attribution
	top unset !important
	bottom 0 !important
	border-top-left-radius 4px !important
	border-bottom-left-radius 0 !important

button.ol-zoom-in
button.ol-zoom-out
	font-size 1.3em

menuwidth = 250px

// UI LAYOUT

#app
#ui
	position absolute
	left 0
	top 0
	bottom 0
	right 0
	pointer-events none
	> *
		pointer-events all

.wrapper
	position fixed
	left 0
	top 0
	right 0
	bottom 0
	display flex
	flex-direction column

.mapareawrapper
	flex-grow 1
	z-index 0
	position relative

.timelinewrapper
	flex-grow 0
	z-index 1
	width 100%

.sidebaropen .mapareawrapper
.sidebaropen .timelinewrapper
.sidebaropen .tableview
	width calc( 100% - 600px )

#mapcontainer
.fullview
	pointer-events all

.topbar
	position fixed
	left 0
	top 0
	right 0
	display flex
	justify-content space-between
	pointer-events none
	> *
		pointer-events all

.is-mobile .topbar
	background: linear-gradient(to bottom, rgba(0, 0, 0, 0.05), transparent);
	flex-wrap wrap

.leftui
	position absolute
	left 0
	top var(--menubariconsize)
	margin-top 1px
	bottom 0
	pointer-events none
	display flex
	flex-direction column
	z-index 2
	> *
		pointer-events all

.is-mobile .leftui
	bottom 0
	z-index 11

// .leftui
// 	position fixed !important
// 	bottom 0
// 	z-index 1000 !important



// MENU WINDOW

.menuwindows
	pointer-events none
	flex-grow 1
	align-self flex-start
	overflow hidden

.menuwindow
	max-height 100%
	pointer-events all
	display flex
	flex-direction column
	background #fff
	box-shadow 2px 2px 4px 0 #00000022
	padding 0
	min-width 225px
	margin-right 6px

.is-mobile .menuwindow
	max-width 80vw !important

.menuwindow-header
	flex-grow 0
	flex-shrink 0
	display flex
	flex-direction row
	align-items center
	justify-content space-between
	height 42px
	padding 0 12px 0
	margin 0
	border-bottom var(--thinline)
	h3
		font-size 12pt
		color #000000bb
		margin 0 0 0

.menuwindow-content
	overflow-y auto
	overflow auto
	margin 0
	padding 6px 6px
	h4
		font-weight bold
		padding-top 6px
		margin 2em 4px .5em
		margin .5em 4px .5em
		display block
		color var(--menusectionheadercolor)
		font-size 80%
		font-size 9pt
		text-transform uppercase
		border-top var(--thinline)
	& > h4:first-of-type
		border-top none
		margin-top 0
		padding-top 6px
	.note
		// opacity .8
		font-size 9pt
		margin-bottom 6px
		em
			font-style normal
			// font-weight bold


// MENU ITEM

.menuitem
	cursor pointer
	width 100%
	margin 0 0
	padding 6px 6px
	min-height 28px
	border-radius 3px
	border-radius 14px
	font-size 9pt
	overflow hidden
	white-space nowrap
	text-overflow ellipsis
	user-select none

.menuitem.hasselectedbelow
	border-bottom-left-radius 0
	border-bottom-right-radius 0
.menuitem.selected + .menuitem.selected
	border-top-left-radius 0
	border-top-right-radius 0

.menuitem.selected
	background var(--activecolorgrey)

.menuwindow-button-row
	display flex
	gap 5px
	flex-direction row
	margin-right -5px

.menuwindow-button
	flex-grow 1
	flex-basis 0
	flex-shrink 0
	border-radius 5px
	background var(--uibrighter)
	border 1px solid #00000022
	border-top 1px solid #00000011
	box-shadow 0 0 2px 2px #00000011
	box-shadow 0 0 2px 2px #00000008
	padding 4px 5px
	white-space nowrap
	cursor pointer
	font-size 90%
	user-select none
	&.active
		background: var(--activecolor)
	&.icon:after
		content ''
		display inline-block
		width 1.1em
		height 1.1em
		margin-right .05em
		margin-left .3em
		margin-bottom -.2em
		background-repeat no-repeat
		background-position center
		background-size 100% 100%
	&.legende:after
	&.extended:after
		background-image url(/img/legende.png)
		opacity 1
	&.transparent:after
		background-size 80% 80%
		opacity .7
		background-image url(/img/transparentr.png)
	&.pinbookmarks:after
		background-size 100% 100%
		opacity .7
		background-image url(/img/pinned.svg)


.itemrow
	display: flex
	margin: 0
	flex-direction: row


@media (max-width: 600px)
	.itemrow
		gap .2em
		flex-direction column


// SETTINGS

.settings-group:first-of-type
	padding-top .4empx
.settings-group
	padding 8px 8px
	position relative
	font-size 12px
	label.small
		font-size 80%
		margin 0 0 4px
		// opacity .6
		cursor default
	.settingslabel
		margin 0 0 4px

.settings-group + .settings-group
	margin-bottom 10px

.settings-item
	margin 4px 0 0
.settings-item + .settings-item
	margin-top 8px


input[type=checkbox]
input[type=radio]
	width 14px
	height 14px
	margin 0
	position relative
	top 2px
	accent-color: #888
	cursor pointer
input[type=radio]
	top 1px

.settings-item
	input + label
		padding-left 6px
		cursor pointer
		display inline-block


button
	border none
	border-radius 1em
	height 2em
	min-width 8em
	padding 0 1em
	white-space nowrap
	width fit-content
	min-width: 120px
	align-self flex-start
	cursor pointer
	background var(--activecolorgrey)

button:active
	background var(--activecolorgreydarker)

button.danger
	background #f5d2d2

button.success
	background #bde5c5

// button.active
	// background var(--activecolorgreydarker)

// MANAGEMENT

.split
	display flex
	justify-content space-between

.management-panel
	padding 14px

.management-window-size-mid
	box-sizing border-box
	width 400px
	max-width calc(100vw - 32px)

.management-form
	display grid
	gap 12px
	align-items start
	font-size 9pt

.management-field
	display grid
	gap 4px

.management-field input
.management-field select
.management-field textarea
	box-sizing border-box
	min-width 0
	padding 7px
	border 1px solid #00000033
	border-radius 4px
	background #fff
	font inherit

.management-readonly-value
	padding 7px
	border 1px solid #00000033
	border-radius 4px
	background #00000008

.management-field input:disabled
	background #f1f1f1
	color #555

.management-note
	margin 0
	padding 6px 8px
	border-radius 4px
	font-size 9pt
	line-height 1.4

.management-notice
	opacity .65
	background #00000008

.management-error
	color #b52323
	background #b5232314

.management-success
	color #177143
	background #17714314

.management-section-title
	margin 0
	font-size 11pt

.management-actions
	display flex
	flex-wrap wrap
	gap 8px
	align-items center

.spacebetween
	width 100%
	justify-content space-between

.management-location-select
	display grid
	gap 8px

.location-select-toggle
	min-width 0

.location-select-dropdown
	border 1px solid #00000022
	border-radius 4px
	background #fff
	overflow hidden

.location-select-modal
	display grid
	gap 10px
	height calc(70vh - 36px)
	max-height calc(70vh - 36px)
	grid-template-rows auto minmax(0, 1fr)

.location-select-modal .location-select-dropdown
	display flex
	min-height 0
	flex-direction column

.location-select-modal .location-select-options
	min-height 0
	max-height none
	overflow-y auto

.location-select-search
	box-sizing border-box
	width 100%
	padding 7px
	border 0
	border-bottom 1px solid #00000022
	font inherit

.location-select-options
	max-height 210px
	overflow-y auto

.location-select-option
	display flex
	width 100%
	min-width 0
	justify-content space-between
	gap 12px
	padding 7px 10px
	border-radius 0
	background #fff
	text-align left

.location-select-option.selected
	background var(--activecolorgreymid)

.location-select-label
	display flex
	align-items center
	min-width 0

.location-select-selected
	display grid
	gap 2px

.location-select-selected-item
	padding 5px 8px
	border-radius 3px
	background #00000008
	font-size 9pt

button.management-danger
	background #b52323
	color #fff

// FULL VIEW

.fullview
	background var(--windowbg)
	background #fff
	position fixed
	width 100vw
	height 100vh
	z-index 300

.fullview.iframe
	padding 10px 6px 0

.fullview.single
	padding 12px 0 48px
	overflow-y scroll

.fullview.embed
	.sideview
		margin-right 0


.fullview.embed
.fullview.qrcode
	.scrollcontent
		box-shadow -2px 0 4px #00000011 inset
		background var(--windowbg)
	.sideview
		border-right 1px solid #00000011
		z-index 10
		box-shadow 2px 0 4px #00000011


.fullviewheader
	background var(--windowbg)
	flex-grow 0
	flex-shrink 0
	min-height 80px
	border-bottom var(--separatorline)
	width 100%
	z-index 2
	display flex
	flex-direction row
	align-items center
	justify-content center

.windowrow
	display flex
	flex-direction row
	width 100%
	height 100%
	overflow hidden
	> .scrollcontent
		padding-right 36px

.windowcol
	display flex
	flex-direction column
	width 100%
	height 100%
	overflow hidden
	// padding-right 10px

.row
	display flex
	flex-direction row
.col
	display flex
	flex-direction column



// .sidebar .windowcol
	// padding-right 54px

.scrollcontent
	flex-grow 1
	overflow-y auto
	padding 0 0 0
	overflow-x hidden


// SIDEBAR

.sidebar
	pointer-events all
	background #fff
	position fixed
	right 0
	top 0
	bottom 0
	width 600px
	max-width 100%
	z-index 100
	box-shadow -2px 0 4px 0 #00000022
	box-shadow -3px 0 6px 0 #00000018
	overflow visible

.sidebar.fullwidth
	width 100vw

@media (max-width: 900px) // mobile full width sidebar
	.sidebar
		width 100vw
		z-index 300
		.windowrow .scrollcontent
			padding-right 0


// MODAL WINDOW


.modalouter
	position fixed
	left 0
	top 0
	right 0
	bottom 0
	display flex
	align-items center	// ← CENTER vertically when it fits!
	justify-content center
	pointer-events none
	z-index 200
	padding 0	// ← optional: give a little breathing room

.modalwindow
	width 800px
	height auto
	max-width calc(100vw - 2em)
	pointer-events all
	position relative
	margin 8px
	background var(--windowbg)
	box-shadow 0 1px 6px 0 #00000044

.modalinner
	max-height 100vh
	padding 0 32px
	overflow-y auto


.modalwindow.info
	background white



// WINDOW BUTTONS

.windowbuttons
	right 0
	top 0
	bottom 0
	margin 0
	width 36px
	padding 6px 0 8px 2px
	flex-grow 0
	display flex
	flex-direction column
	align-items center
	justify-content flex-start
	border-left 1px solid #eee
	position absolute

.windowbuttons.left
	right unset
	left 0
	padding 6px 2px 8px 0

.windowbuttons.row
	flex-direction row
	width unset
	height 36px
	gap 8px
	padding 0 6px 0 4px

.windowbuttons.row.left
	padding 0 0 0 4px

.windowbuttonsinner
	pointer-events all
	display flex
	flex-direction column
	gap 10px

.windowbuttons.plain
	background transparent
	box-shadow none
	border-left 1px solid transparent
	bottom unset
	&:after
		display none
.windowbuttons.small
	margin-top -2px
	margin-right -4px
	.iconbutton
		width 16px
		height 16px
		opacity .4
		&:hover
			opacity 1

@media (max-width: 900px)
	.windowbuttons
		background transparent
		box-shadow none
		bottom unset
		z-index 100
		border none
		&:after
		.switchfullwindow
		.more
			display none

.fullview .windowbuttons
	z-index 1000

.iconbutton
	width 24px
	height 24px
	background-size 100% 100%
	background-position center
	background-repeat no-repeat
	opacity .8
	transition opacity linear .05s
	position relative
	cursor pointer
	&:hover
		opacity .45

.iconbutton.light
	opacity .45
	&:hover
	&.active
		opacity .8

.iconbutton.bookmark
	background-image url(/img/bookmarkfill.svg)
	background-size 110%
	opacity .2
.iconbutton.bookmark.active
	opacity .8

.iconbutton.more
	background-image url(/img/morev.png)
.iconbutton.more
	background-image url(/img/morev.png)
.iconbutton.settings
	background-image url(/img/settings_black.png)
	background-size 105%
.iconbutton.filter
	background-image url(/img/filter_black.png)
	background-size 110%
.iconbutton.close
	background-image url(/img/close.png)
	background-size 120%
.iconbutton.switchfullwindow
	background-image url(/img/dock_left.png)
	background-size 94%
	background-size 90%
.sidebar.fullwidth .iconbutton.switchfullwindow
	background-image url(/img/dock_right.png)

// POPOVER

///

.separator
	display inline-block
	margin 0 .3em
	opacity .4

.separator::before
	content '-'

hr
	border none
	border-bottom var(--thinline)
	margin .8em var(--sidebartextmargin)

.chartouterframe
	margin 1.2em 0 .4em
	position relative
	user-select none
	.scrollframe
		background transparent

.chartheader
	display flex
	align-items baseline
	justify-content space-between
	margin-top -.4em
	h3
		margin 0 0 .8em
		margin-left var(--sidebartextmargin)
		font-size 10pt
		color #000000bb
	.latestdate
		font-size 8pt
		font-weight bold
		font-weight normal
		margin-left 1em
		color #000000cc
		&.warning
			color red

.chart-time
	display block
	position relative

.heatmap .chart-time
	border-top 1px solid transparent
.schichten .chart-time
	border-top 1px solid #eee

.value
	font-weight bold
.unit
	display inline-block
	margin-left .15em

///


.hoverline
	position absolute
	top 0
	bottom 0
	width 0
	border-left 1px dotted #00000088
	pointer-events none
	z-index 100
// .hovertriangle
// 	margin-left -3.5px
// 	top -8px
// 	position absolute
// 	border-top 8px solid var(--timelinedatebg)
// 	border-left 5px solid transparent
// 	border-right 5px solid transparent
// 	opacity .6

// .hovertrianglebottom
// 	margin-left -3.5px
// 	bottom -8px
// 	position absolute
// 	border-bottom 8px solid var(--timelinedatebg)
// 	border-left 5px solid transparent
// 	border-right 5px solid transparent
// 	opacity .6

.hover-dot
	stroke-width 0
	pointer-events none

// MARKER

.marker-outer
	opacity 0
.marker-outer.telemetryloaded
	opacity 1

.map-marker
	position: absolute;
	cursor pointer
	width: 20px;
	height: 20px;
	user-select: none
	transform: translate(-50%, -50%);
.map-marker.faux
	transform none
	position relative


.marker-container.mouseover
	z-index 1000

.marker-container
	overflow visible

// .marker-outer.filtered
// 	opacity 0
// 	pointer-events none
// 	*
// 		pointer-events none

.map-marker.alle
	width auto
	height auto
	filter: drop-shadow(0 0 2px rgba(0,0,0,.2));
	transform: translate(-50%, -4px);
	.schichten
		display flex
		flex-direction column
		border-radius 12px
		overflow hidden
		border 2px solid transparent
		.schicht
			display flex
			flex-direction row
			align-items flex-end
			justify-content flex-end
			padding 2px 6px 2px 9px
			&:first-child
				padding-top 4px
			&:last-child
				padding-bottom 4px
			.value
				flex-grow 0
				flex-shrink 0
				flex-basis auto
				white-space nowrap
				font-size 10pt
				text-align right
			.unit
				font-weight normal
				opacity .8
				font-size 90%
		.schicht + .schicht
			margin-top -1px

.map-marker.einzeln
	width auto
	height auto
	.schicht
		padding 2px 6px 2px 8px
		display flex
		flex-direction row
		border-radius 15px
		background wasserblau
		filter: drop-shadow(0 0 2px rgba(0,0,0,.2));
		border 2px solid transparent
		.value
			white-space nowrap
			.unit
				font-weight normal
				opacity .8
				font-size 95%

.map-marker.kreis
	width auto
	height auto
	.schichten
		border 2px solid #00000033
		filter: drop-shadow(0 1px 2px rgba(0,0,0,.3));
		width 36px
		height 36px
		border-radius 50%
		overflow hidden
		display flex
		flex-direction column
		cursor pointer
		.schicht
			flex-basis 50%
			width 100%
			flex-grow 1

.map-marker.property .propertyicon
	position relative
	left -8px
	top -8px
	width 36px
	height 36px
	filter drop-shadow(0 1px 2px rgba(0,0,0,.3))
	.icon
		border 2px solid #00000033
		border-radius 50%


.map-marker.selected .filtered
.map-marker.selected .disabled
.map-marker.selected .schichten
.map-marker.einzeln.selected .schicht
.map-marker.property.selected .propertyicon .icon
	border 2px solid #888
	outline 2px solid #ffffffdd
	filter: drop-shadow(0 1px 2px rgba(0,0,0,.45))

.map-marker .disabled
	width auto
	height auto
	width 20px
	height 20px
	border-radius 50%
	background #aaa
	border 1.5px solid #00000022
	filter: drop-shadow(0 1px 2px rgba(0,0,0,.2));
	pointer-events none
	font-size 12pt
	display flex
	align-items center
	justify-content center
	color #000
	text-align centers

.map-marker .disabled.warning
	background #ea8d8dff



.marker-outer.filtered
	pointer-events none
	display none

.map-marker .filtered
	pointer-events none
	display none
	width auto
	height auto
	// box-shadow 0 1px 2px 2px #00000011
	width 6px
	height 6px
	margin-left 7px
	border-radius 50%
	background #666
	// border 2px solid #00000077
	pointer-events none
// .map-marker .filtered
// 	width auto
// 	height auto
// 	// box-shadow 0 1px 2px 2px #00000011
// 	width 12px
// 	height 12px
// 	margin-left 4px
// 	border-radius 50%
// 	background #888
// 	// border 2px solid #00000077
// 	pointer-events none
